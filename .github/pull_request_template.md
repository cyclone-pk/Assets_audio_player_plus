## What

<!-- One or two sentences: what does this PR do? -->

## Why

<!-- The user-visible or technical reason. Link the issue it closes. -->

Closes #

## How it was tested

<!-- Check every box that applies and delete the rest. -->

- [ ] `dart format .` passes
- [ ] `flutter analyze` passes on the plugin
- [ ] `flutter analyze` passes on `assets_audio_player_plus_web`
- [ ] `flutter analyze` passes on `example`
- [ ] `flutter test` passes
- [ ] Example app runs on:
  - [ ] Android
  - [ ] iOS
  - [ ] macOS
  - [ ] Web (JS) — `flutter run -d chrome`
  - [ ] Web (WASM) — `flutter build web --wasm`
  - [ ] Windows / Linux

**Flutter version used:**
```
<paste output of `flutter --version`>
```

## Public API changes

<!-- "None" if this is a pure bug fix. Otherwise list added/removed/changed
     classes, methods, parameters, and whether they are breaking. -->

None.

## Screenshots / recordings

<!-- Required for any UI change. -->

## Checklist for the reviewer

- [ ] New/changed public APIs have doc comments
- [ ] `CHANGELOG.md` updated with a user-facing line (unless internal-only)
- [ ] Relevant demo in `example/` was updated or added
